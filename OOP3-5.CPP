#include<iostream.h>
#include<conio.h>
void main()
{ clrscr();
  int max(int &,int &);
  int a,b,c,d;
  cout<<"enter the 4 numbers"<<endl;
  cin>>a>>b>>c>>d;
  great=max(a,max(b,max(c,d)));
  great=1000;
  cout<<"\n the max is"<<grea
  getch();
  }
  max(int & a,int &b)
  {
     if(a>b)
       return a;
     else
     return b;
     }
